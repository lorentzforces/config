session_name: 'updates'
start_directory: '${HOME}'
windows:
  - layout: 'even-horizontal'
    options:
      automatic-rename: 'on'
    panes:
      - sleep_after: 1
        focus: true
        shell_command:
        - 'package-update-check && page ~/mine/updatable-packages.txt'
      - 'blank'
