LF
	mkdir
	delete
	working dir following

	nvim integration

	KEYBINDS?

startup
	What on earth is going on with dbus? When using startx the DBUS_SESSION_BUS_ADDRESS environment variable was either getting unset or wasn't being passed through to the X session. It would be nice to have a desktop environment that doesn't depend on whatever magic lightdm is doing behind the scenes (there's a lot of it).
	Ideally, this also lets us run some of our basic xrandr stuff out of the gates.

	see also: awesome

awesome
	Clean up code in general.
	Move initialization of programs out of awesome where possible (see startup). Sometimes the window manager needs to be restarted, and this results in some "should only have one instance running" programs getting duplicated.
